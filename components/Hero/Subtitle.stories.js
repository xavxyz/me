import { storiesOf } from '@kadira/storybook';
import React from 'react';
import Subtitle from './Subtitle';

storiesOf('👋 Hero - Subtitle', module).add('default', () => (
  <Subtitle>Building webapps for your delight</Subtitle>
));
