import { storiesOf } from '@kadira/storybook';
import React from 'react';
import Title from './Title';

storiesOf('👋 Hero - Title', module).add('default', () => <Title>Javascript Engineer</Title>);
